const express = require("express");
const router = express.Router();
const AttandanceController = require("../controllers/attendanceController")

// router.get("/", AttandanceController.getAllAttandances);

module.exports = router;