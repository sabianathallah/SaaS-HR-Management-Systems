const express = require("express");
const router = express.Router();
const AttandanceController = require("../controllers/attandanceController")

// router.get("/", AttandanceController.getAllAttandances);

module.exports = router;